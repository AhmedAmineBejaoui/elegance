{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "functions": {
    "api/[...all].js": {
      "runtime": "nodejs20.0.0"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...all]" },
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "env": {
    "ROLLUP_SKIP_NODEJS_NATIVE": "1"
  }
}
